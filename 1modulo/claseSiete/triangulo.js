let readlineSync=require("readline-sync");
let baseTriangulo=readlineSync.questionInt("Ingrese la base del triangulo: ");
let alturaTriangulo=readlineSync.questionInt("Ingrese la altura del triangulo: ");
function areaTriangulo (base),(altura) {
resultado=(base)*(altura);
return=resultado;
}